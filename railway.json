{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "DATABASE_URL=postgresql://x:x@localhost/x pnpm --filter @lifeos/web exec prisma generate && pnpm --filter @lifeos/web build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @lifeos/web exec prisma migrate deploy && node apps/web/build/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
